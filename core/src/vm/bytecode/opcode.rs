#[derive(Debug, Clone, PartialEq)]
pub enum Opcode {
    Constant(u16),
    Add,
    Sub,
    Mul,
    Div,
    Mod,
    Negate,
    Not,
    Equal,
    StrictEqual,
    LessThan,
    GreaterThan,
    GetLocal(u16),
    SetLocal(u16),
    GetGlobal(u16),
    SetGlobal(u16),
    JumpIfFalse(u16),
    Jump(u16),
    Loop(u16),
    Call(u8),
    Return,
    Pop,
    Print,
    Nil,
    True,
    False,
    GetProperty,
    SetProperty,
    CreateObject,
    CreateArray,
    Typeof,
    Nop,
    GetPropertyIC(u16),
}
